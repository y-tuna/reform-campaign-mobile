# 📱 유세 비서 모바일 앱 기획서

> **버전**: 2.0.0
> **작성일**: 2024-12-22
> **상태**: 개발 진행 중
> **참조**: reform-campaign-spec-v2.2, v2.3, 참치 정치서비스 구조(비서 중점)

---

## 1. 앱 개요

### 컨셉
> **"후보자 유세 비서 앱"** - POI 기반 스마트 유세 일정 + 선거법 챗봇

### 타겟 사용자
- **후보자 전용** (예비후보자 / 후보자)
- 연령대: 2030 ~ 60+ (Senior 모드 지원)

### 웹과의 역할 분담

| 기능 | 웹 | 모바일 |
|------|:---:|:------:|
| 유세 일정/POI | 정책 설정 | ✅ 실행 (메인) |
| 대시보드 | ✅ 상세 분석 | ✅ 요약 |
| 챗봇 | ✅ 상세 문서 | ✅ 간편 Q&A |
| 서류 제출 | ✅ | ❌ |
| 공보물 생성 | ✅ | ❌ |
| 관리자 기능 | ✅ | ❌ |

---

## 2. 핵심 기능 (MVP)

| 우선순위 | 기능 | 설명 |
|---------|------|------|
| 🔴 P0 | **오늘의 유세** | POI 추천 + 지도 동선 + 시간별 스케줄 |
| 🔴 P0 | **선거법 챗봇** | RAG 기반 선거법/선관위 질의 챗봇 |
| 🔴 P0 | **푸시 알림** | 유세 리마인더, 긴급 공지 |
| 🟠 P1 | **대시보드** | 활동 통계 + GPS 히트맵 (모듈화) |
| 🟠 P1 | **프로필 설정** | 종교, 이동수단, 스케줄강도, 접근성 |
| 🟡 P2 | **공지사항** | 당 공지 확인 |

---

## 3. 화면 구성

### 3.1 네비게이션 구조

```
하단 탭 네비게이션 (5개)
├── 🏠 홈 (오늘의 유세)     ← 메인
├── 📊 대시보드            ← 활동 통계 + 히트맵
├── 💬 챗봇               ← 선거법 Q&A
├── 🔔 알림               ← 공지사항
└── 👤 프로필             ← 선호 설정 포함
```

### 3.2 화면 목록

#### 1) 홈 (오늘의 유세) ⭐ 메인

```
┌────────────────────────────┐
│  12월 22일 일요일           │
│  오늘의 유세 3곳            │
├────────────────────────────┤
│  ┌──────────────────────┐  │
│  │      [지도 뷰]        │  │
│  │   📍──📍──📍         │  │
│  │    동선 표시          │  │
│  └──────────────────────┘  │
├────────────────────────────┤
│  📍 09:00 강남역 3번출구    │
│     예상 접촉: 120명        │
│     [시작] [네비] [미루기]  │
├────────────────────────────┤
│  📍 11:00 삼성역 환승통로   │
│     예상 접촉: 85명         │
│     [시작] [네비] [미루기]  │
├────────────────────────────┤
│                      [+]   │
└────────────────────────────┘
```

**기능:**
- POI 기반 자동 추천 일정
- 지도에 동선 표시 (react-native-maps)
- 예상 접촉 수 표시
- 일정 시작/완료 (GPS 트래킹)
- 수동 일정 추가 (+)

**일정 카드 액션:**
- `시작`: GPS 트래킹 시작, 상태 → started
- `네비`: 외부 지도앱 연동 (네이버/카카오맵)
- `미루기`: 다음 시간대로 연기
- `완료`: 활동 종료, 메모/사진 첨부 (선택)

---

#### 2) 대시보드 (모듈화)

```
┌────────────────────────────┐
│  📊 내 활동 현황            │
├────────────────────────────┤
│  [Module A: 활동 통계]      │
│  ┌──────────────────────┐  │
│  │ 이번 주 유세: 12회    │  │
│  │ 총 접촉: 1,240명      │  │
│  │ 이동 거리: 45km       │  │
│  └──────────────────────┘  │
│                            │
│  📈 방문 카테고리           │
│  ┌──────────────────────┐  │
│  │ 지하철 ████████ 45%  │  │
│  │ 시장   ████░░░░ 30%  │  │
│  │ 학원가 ███░░░░░ 25%  │  │
│  └──────────────────────┘  │
├────────────────────────────┤
│  [Module B: GPS 히트맵]     │
│  ┌──────────────────────┐  │
│  │     🟡🟠🔴            │  │
│  │   유세 시간 히트맵    │  │
│  │   (체류 시간 기반)    │  │
│  └──────────────────────┘  │
└────────────────────────────┘
```

**모듈화 설계:**
- `Module A`: 활동 통계 (필수)
  - 주간/월간 유세 횟수
  - 총 예상 접촉 수
  - 이동 거리
  - 방문 카테고리 분포
- `Module B`: GPS 히트맵 (선택)
  - POI 알고리즘/데이터에 따라 On/Off
  - 체류 시간 기반 히트맵

---

#### 3) 챗봇 (선거법 Q&A)

```
┌────────────────────────────┐
│  💬 선거법 도우미          │
├────────────────────────────┤
│                            │
│  🤖 안녕하세요! 선거법에   │
│     관해 궁금한 점을       │
│     물어보세요.            │
│                            │
│  ┌──────────────────────┐  │
│  │ 👤 명함은 몇 장까지   │  │
│  │    배포할 수 있나요?  │  │
│  └──────────────────────┘  │
│                            │
│  ┌──────────────────────┐  │
│  │ 🤖 공직선거법 제112조 │  │
│  │ 에 따르면 명함은...   │  │
│  │                      │  │
│  │ 📎 관련 조항:         │  │
│  │ • 제112조 (명함배부)  │  │
│  │ [자세히 보기]         │  │
│  └──────────────────────┘  │
│                            │
├────────────────────────────┤
│  ┌──────────────────────┐  │
│  │ 질문을 입력하세요...  │ ▶│
│  └──────────────────────┘  │
└────────────────────────────┘
```

**기능:**
- RAG/CAG 기반 검색 증강 생성
- 선거법 + 선관위 질의응답 데이터
- 관련 법령 조항 인용 표시
- [자세히 보기] → 웹 리다이렉트

**기술 구조:**
- 싱글턴 API 엔드포인트 호출
- 대화 히스토리 유지 (세션 내)
- 출처/근거 표시 필수

---

#### 4) 알림/공지

```
┌────────────────────────────┐
│  🔔 알림                   │
├────────────────────────────┤
│  오늘                      │
│  ├─ 🔴 [긴급] 선거법 개정  │
│  │   2시간 전              │
│  ├─ 📅 14:00 삼성역 유세   │
│  │   30분 전 리마인드      │
│  └─ 📢 이번 주 유세 지침   │
│      어제                  │
├────────────────────────────┤
│  이번 주                   │
│  └─ 📢 당 정책 브리핑      │
└────────────────────────────┘
```

**기능:**
- 푸시 알림 목록
- 유세 리마인더 (30분/10분 전)
- 당 공지사항
- 읽음/안읽음 표시
- severity 기반 색상 (info/warn/critical)

---

#### 5) 프로필 (선호 설정)

```
┌────────────────────────────┐
│  👤 홍길동                 │
│     서울 강남구 예비후보    │
├────────────────────────────┤
│  ⚙️ 유세 선호 설정         │
│  ┌──────────────────────┐  │
│  │ 종교 시설             │  │
│  │ [제외없음 ▼]          │  │
│  │                      │  │
│  │ 이동 수단             │  │
│  │ [🚗] [🛻] [🚴] [🚶]   │  │
│  │                      │  │
│  │ 스케줄 강도           │  │
│  │ [빡셈] [보통] [여유]   │  │
│  └──────────────────────┘  │
├────────────────────────────┤
│  📱 접근성 설정            │
│  ┌──────────────────────┐  │
│  │ 큰 글씨 모드    [OFF] │  │
│  │ 다크 모드      [OFF] │  │
│  └──────────────────────┘  │
├────────────────────────────┤
│  ℹ️ 앱 정보                │
│  ❓ 도움말                 │
│  🚪 로그아웃               │
└────────────────────────────┘
```

**선호 설정 항목:**
- **종교**: 제외없음 / 특정 종교 제외 / 특정 종교만
- **이동수단**: car / pickup / bike / walk
- **스케줄 강도**: hard / normal / light
- **큰 글씨 모드**: Senior UI 활성화
- **다크 모드**: 테마 전환

---

## 4. 데이터 구조

### POI (유세 위치)
```typescript
interface POI {
  id: string
  name: string              // "강남역 3번출구"
  type: POIType
  location: {
    lat: number
    lng: number
  }
  baseExposure: number      // 기본 노출값
  timeWeights: {            // 시간대별 가중치
    morning: number         // 출근 시간
    noon: number
    evening: number         // 퇴근 시간
  }
  accessibility: number     // 접근성 점수
  metadata?: {
    exits?: number          // 출구 수
    crosswalks?: number     // 횡단보도
    stayArea?: boolean      // 체류 공간 유무
  }
}

type POIType =
  | 'subway'      // 지하철
  | 'bus'         // 버스환승
  | 'market'      // 시장
  | 'school'      // 학원가
  | 'facility'    // 시설/복지관
  | 'religious'   // 종교시설
  | 'other'
```

### CampaignSchedule (유세 일정)
```typescript
interface CampaignSchedule {
  id: string
  userId: string
  poi: POI
  date: string              // YYYY-MM-DD
  startTime: string         // HH:mm
  endTime?: string
  estimatedExposure: number // 예상 접촉 수
  status: ScheduleStatus
  gpsTrack?: GeoPoint[]     // GPS 트래킹 기록
  memo?: string
  createdAt: string
  updatedAt: string
}

type ScheduleStatus =
  | 'planned'    // 예정
  | 'started'    // 진행 중
  | 'done'       // 완료
  | 'skipped'    // 건너뜀
```

### Profile (프로필)
```typescript
interface UserProfile {
  id: string
  name: string
  district: string          // 선거구
  candidateType: 'preliminary' | 'official'  // 예비/정식 후보
  preferences: ProfilePreferences
  createdAt: string
  updatedAt: string
}

interface ProfilePreferences {
  religion: ReligionPref
  mobility: MobilityType
  intensity: IntensityLevel
  fontSize: 'default' | 'large'
  darkMode: boolean
}

type ReligionPref =
  | { type: 'none' }                    // 제외 없음
  | { type: 'exclude', list: string[] } // 특정 종교 제외
  | { type: 'only', list: string[] }    // 특정 종교만

type MobilityType = 'car' | 'pickup' | 'bike' | 'walk'
type IntensityLevel = 'hard' | 'normal' | 'light'
```

### ChatMessage (챗봇)
```typescript
interface ChatMessage {
  id: string
  role: 'user' | 'assistant'
  content: string
  sources?: LawReference[]  // 인용 법령
  createdAt: string
}

interface LawReference {
  code: string              // "공직선거법 제112조"
  title: string
  summary: string
  url?: string              // 상세 페이지 URL
}
```

### Notification (알림)
```typescript
interface Notification {
  id: string
  userId: string
  type: 'schedule_reminder' | 'broadcast' | 'system'
  title: string
  body: string
  severity: 'info' | 'warn' | 'critical'
  isRead: boolean
  createdAt: string
}
```

### ActivityStats (대시보드)
```typescript
interface ActivityStats {
  period: 'week' | 'month'
  totalVisits: number
  totalExposure: number
  totalDistance: number     // km
  categoryBreakdown: {
    [key in POIType]?: number
  }
  dailyStats: DailyStat[]
}

interface DailyStat {
  date: string
  visits: number
  exposure: number
  distance: number
}

// GPS 히트맵용
interface HeatmapData {
  points: HeatmapPoint[]
}

interface HeatmapPoint {
  lat: number
  lng: number
  weight: number            // 체류 시간 기반
}
```

---

## 5. 기술 스택

| 영역 | 기술 |
|------|------|
| Framework | Expo 53 (React Native) |
| Language | TypeScript 5 |
| Navigation | React Navigation 7 (Bottom Tabs) |
| State | Zustand |
| Maps | react-native-maps |
| Backend | Supabase |
| Push | Expo Notifications |
| Charts | react-native-chart-kit (대시보드용) |

---

## 6. 개발 로드맵

### Phase 1: MVP Core (현재)
- [x] 프로젝트 초기화
- [ ] 하단 탭 네비게이션 (5탭)
- [ ] 홈 화면 (오늘의 유세)
  - [ ] 지도 뷰 (react-native-maps)
  - [ ] 일정 카드 리스트
  - [ ] 일정 상태 관리 (시작/완료/미루기)
- [ ] 프로필 화면 + 선호 설정
- [ ] Supabase 연동

### Phase 2: 챗봇 + 대시보드
- [ ] 챗봇 화면 UI
- [ ] RAG API 엔드포인트 연동
- [ ] 대시보드 (Module A: 활동 통계)
- [ ] 대시보드 (Module B: GPS 히트맵) - 선택

### Phase 3: 알림 + 마무리
- [ ] 알림/공지 화면
- [ ] 푸시 알림 설정
- [ ] 유세 리마인더
- [ ] 로그인/인증
- [ ] Senior 모드 (큰 글씨)

---

## 7. 폴더 구조 (예정)

```
apps/mobile/
├── app/                    # Expo Router (예정)
│   ├── (tabs)/
│   │   ├── index.tsx       # 홈 (오늘의 유세)
│   │   ├── dashboard.tsx   # 대시보드
│   │   ├── chat.tsx        # 챗봇
│   │   ├── notifications.tsx
│   │   └── profile.tsx
│   └── _layout.tsx
├── components/
│   ├── schedule/           # 일정 관련
│   ├── map/               # 지도 관련
│   ├── chat/              # 챗봇 관련
│   ├── dashboard/         # 대시보드 모듈
│   └── ui/                # 공통 UI
├── stores/                # Zustand stores
├── hooks/                 # Custom hooks
├── types/                 # TypeScript types
├── utils/                 # 유틸리티
├── constants/             # 상수
└── services/              # API 서비스
```

---

## 8. 실행 방법

```bash
# 모바일 앱 디렉토리로 이동
cd apps/mobile

# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 웹에서 확인
npm run web
```

- **웹 브라우저**: http://localhost:19006
- **실제 기기**: Expo Go 앱에서 QR 코드 스캔
